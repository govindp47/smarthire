version: '3.8'

services:
  # FastAPI Backend (Pre-built from Docker Hub)
  backend:
    image: govindp47/smarthire-backend:latest
    container_name: smarthire-backend
    environment:
      DATABASE_URL: ${DATABASE_URL}  # From AWS RDS
      SECRET_KEY: ${SECRET_KEY}
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-ada-002}
      VECTOR_STORE_PATH: /app/vector_store
      CORS_ORIGINS: ${CORS_ORIGINS}
      ENVIRONMENT: production
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY}
      AWS_REGION: ${AWS_REGION:-us-east-1}
      S3_BUCKET_NAME: ${S3_BUCKET_NAME}
    ports:
      - "8000:8000"
    volumes:
      - backend_vector_store:/app/vector_store
    networks:
      - smarthire-network
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2G
        reservations:
          cpus: '1'
          memory: 1G

  # React Frontend (Pre-built from Docker Hub)
  frontend:
    image: govindp47/smarthire-frontend:latest
    container_name: smarthire-frontend
    ports:
      - "80:80"
      - "443:443"  # For HTTPS
    depends_on:
      - backend
    networks:
      - smarthire-network
    restart: always
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 512M

volumes:
  backend_vector_store:
    driver: local

networks:
  smarthire-network:
    driver: bridge
